# G6 v8 Fresh C1 Screening Failure - 2026-08-13

Status: `V8_C1_FAILED_CLOSED`

## Observation

Run: `20260813T145417Z_executable_profile_d0f99f50`

Trial: `g6trial_22fe2ff5033b6bc9d42176dd`

- canonical concurrency: `1`;
- real provider generations: `8`;
- submissions: `0/8`;
- required fact-key recall: `0/40`;
- retries: `0`;
- generation ledger: `86 -> 94`;
- hard ceiling: `118`;
- unused authorized capacity preserved: `24` generations.

C2, C4, and C8 were not launched after the universal C1 contract failure.

## Failure corpus

All eight units reached a provider generation and then failed closed while validating the free-form exact-source-quote contract:

- B01: the selected quote matched `soma/tasks/backends.py` twice;
- B02: the selected quote matched `soma/company_kernel/schema.py` twice;
- B03: the selected quote was not an exact substring of `soma/hermes_service_supervisor.py`;
- B04: the selected quote matched `soma/workflows/worker.py` seven times;
- B05: the selected quote was not an exact substring of `soma/worker_adapters/contract.py`;
- B06: the selected quote was not an exact substring of `soma/worker_substrate/dispatch.py`;
- B07: the selected quote matched `soma/project_scope/store.py` three times;
- B08: the selected quote was not an exact substring of `soma/run_store.py`.

No invalid output was repaired, normalized, retried, or counted as a submission.

## Finding

v8 removed the v7 support/opposition ambiguity, but provider-authored exact unique source quotes are not reliable enough to serve as evidence identity.

The next versioned repair target is support-only packet-bound line spans:

- provider chooses `source_path`, `start_line`, and `end_line` only;
- the provider source view uses deterministic one-based numbered lines;
- Soma validates the range against the frozen packet;
- Soma injects the exact source hash, locator, and verbatim excerpt mechanically;
- no opposition relation is reintroduced;
- no free-form quote is used as an identity primitive.

This design removes both measured structural failure classes: v7 polarity ambiguity and v8 quote-copy/uniqueness ambiguity.

## Architecture correction

The failure also exposed an implementation drift from the canonical G3.4 boundary. The worker is a reasoning actor and Soma is a durable/mechanical system, not a semantic judge.

For the next contract:

- the worker decides the answer and which assigned source locations support it;
- Soma checks only that the referenced frozen file/range is mechanically real and preserves the exact artifact/locator;
- Soma does not determine whether that passage actually proves the claim;
- Sol performs semantic evidence-precision, unsupported-assertion, contradiction, and uncertainty adjudication after FanIn.

Line locators therefore exist only as durable evidence pointers. They are not a proof-grading system inside Soma.

## Post-observation state

Focused validation run `20260813T152057Z_executable_profile_13c92f8f` passed `33` tests after returning the implementation to the last fully integrated v8 state.

Quota status run `20260813T152603Z_executable_profile_0ea80787`:

- model generations observed: `94`;
- model-generation ceiling: `118`;
- provider send boundaries crossed: `102`;
- unused authorized model generations: `24`.

The remaining 24 generations must not be spent on a partial next-contract screening. A fair fresh C1/C2/C4/C8 screening needs 32 generations under one fixed execution contract, so after the next local contract revision is accepted the required owner ceiling will be at least `126` (`94 + 32`).

## Gate result

`V8 REJECTED AFTER FRESH C1 UNIVERSAL MECHANICAL FAILURE`

`24 AUTHORIZED GENERATIONS PRESERVED`

`NEXT REPAIR TARGET: SUPPORT-ONLY PACKET-BOUND LINE SPANS`

`G6.4 BLOCKED`

`G7 BLOCKED`
